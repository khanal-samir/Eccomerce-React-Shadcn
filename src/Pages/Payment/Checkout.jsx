import { PayNav } from "@/components";
import { ChekoutBody } from "@/components";
const Checkout = () => {
  return (
    <div>
      <PayNav second={true} />
      <ChekoutBody />
    </div>
  );
};

export default Checkout;
